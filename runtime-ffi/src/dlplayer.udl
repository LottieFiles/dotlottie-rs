
namespace dotlottie_utils {
};

interface DotLottiePlayer {
    constructor();
    void tick();
    void load_animation(sequence<u32> buffer, [ByRef] string animation_data, u32 width, u32 height);
};
